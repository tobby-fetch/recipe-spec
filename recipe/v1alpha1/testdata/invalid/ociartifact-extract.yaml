apiVersion: recipe.tobby.dev/v1alpha1
kind: Recipe
metadata:
  name: demo
  version: 1.0.0
spec:
  ingredients:
    - name: model
      kind: OCIArtifact
      ref: registry.example.com/models/demo
      version: 1.2.3
      extract:
        paths: ["etc/**"]
