## Summary

<!-- What does this change do, and why? -->

## Spec sections touched

<!-- List the RECIPE-SPEC.md section(s) (e.g. §8, §9, §11.5) or schema
files this PR implements or changes, and note whether it is a breaking
change to v1alpha1. Write "None" if this is SDK-only or non-normative. -->

## Checklist

- [ ] Tests added or updated, and `mise run test` (`go test -race -count=2 ./...`) passes locally
- [ ] `mise run lint` passes (strict profile, zero suppressions)
- [ ] Every commit is signed off (`git commit -s`) — DCO
- [ ] Examples in `examples/` still validate, or were updated alongside a
      format change
- [ ] Documentation updated (`RECIPE-SPEC.md`, README, package docs) if
      behavior changed
